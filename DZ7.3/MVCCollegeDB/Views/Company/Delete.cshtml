@model MVCworkorderDB.Models.companys

@{

    ViewBag.Title = "Delete";
}
<!DOCTYPE html>
<html>

<head>
    <meta charset="UTF-8" />
    <link rel="stylesheet" href="/style.css" />
    <title>workorder Database</title>

</head>

<body>
    <div class="container">
        <h2>Delete</h2>

        <h3>Are you sure you want to delete this?</h3>
        <div>
            <h4>companys</h4>
            <hr />
            <dl class="dl-horizontal">
                <dt>
                    @Html.DisplayNameFor(model => model.managers.first_name)
                </dt>

                <dd>
                    @Html.DisplayFor(model => model.managers.first_name)
                </dd>

                <dt>
                    @Html.DisplayNameFor(model => model.locations.name)
                </dt>

                <dd>
                    @Html.DisplayFor(model => model.locations.name)
                </dd>

                <dt>
                    @Html.DisplayNameFor(model => model.companys1)
                </dt>

                <dd>
                    @Html.DisplayFor(model => model.companys1)
                </dd>

            </dl>

            @using (Html.BeginForm())
            {
                @Html.AntiForgeryToken()

                <div class="form-actions no-color">
                    <input type="submit" value="Delete" class="btn btn-default" /> |
                    @Html.ActionLink("Back to List", "Index")
                </div>

            }
        </div>
</body>

</html>