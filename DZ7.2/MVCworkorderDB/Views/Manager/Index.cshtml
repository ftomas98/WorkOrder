@{

    ViewBag.Title = "Index";
}

<h2>Login</h2>

@using (Html.BeginForm("Login", "managers", FormMethod.Post))

{
    @Html.TextBox("first_name", null, new { @class = "form-control" })
    ;
    @Html.TextBox("password", null, new { @class = "form-control" })
    ;

    <input type="submit" value="Login" class="btn btn-primary" />

}
